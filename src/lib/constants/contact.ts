/**
 * Ishonch raqami (TZ 3.1) — header ustida va footerda bir xil ko'rinadi.
 *
 * Bekendda bu qiymat uchun uch yo'q, shuning uchun kodda saqlanadi.
 */
export const TRUST_PHONE = '+998 71 200 70 07'

/** `tel:` uchun bo'shliqsiz shakl. */
export const TRUST_PHONE_TEL = `tel:${TRUST_PHONE.replace(/\s/g, '')}`

export const CONTACT_EMAIL = 'info@smart-inklyuziv.uz'
